import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#000000',
    primary: '#E0611D',
    secondary: '#1BACA4',
    error: '#E0611D',
    // surface: '#E0611D'
  },
}
